<li class="postItem">
<article class="postListItem">
   <div class="postTextContainer"> 
    <h2 class="postTitle"><a href="{{post.url}}">{{post.data.title}} </a><h2> 
        <p class="postMeta"> by <span>{{post.data.author}}</span> on <span>{{post.date}}</span></p>
        <p class="postBody"> {{post.data.description}}</p>
        <a href="{{post.url}}" class="btn btn-primary">Read the artilce</a></div>
<div class="postImgContainer">
    <img src="{{post.data.image}}" alt="{{post.data.imageAlt}}" class="postImage" />
    </div>
</article>



    </li>